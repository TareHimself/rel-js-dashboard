import '../../scss/main.scss';

function PermissionsCategory({ style , settings, updateSettings }) {

    return (
        <div className="dashboard-content" style={style || {}}>
            
        </div>
    );
}

export default PermissionsCategory;